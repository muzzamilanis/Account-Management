<UserControl x:Class="AMS.Views.Controls.AmountUnitEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="160"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Margin="0,0,6,0"
                 Text="{Binding RawAmount, ElementName=Root, UpdateSourceTrigger=PropertyChanged}"/>
        <ComboBox Grid.Column="1"
                  ItemsSource="{Binding UnitOptions, ElementName=Root}"
                  SelectedItem="{Binding SelectedUnit, ElementName=Root}"/>
    </Grid>
</UserControl>
